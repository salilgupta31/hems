/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {DataBean} from './data-bean';

describe('DataBean', () => {
  it('should create an instance', () => {
    expect(new DataBean()).toBeTruthy();
  });
});
