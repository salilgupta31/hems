/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {MapDataTO} from './map-data-to';

describe('MapDataTO', () => {
  it('should create an instance', () => {
    expect(new MapDataTO()).toBeTruthy();
  });
});
